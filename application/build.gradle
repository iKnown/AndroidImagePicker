apply plugin: 'com.android.application'
apply from: rootDir.path + '/shared.gradle'

android {
    defaultConfig {
        applicationId 'org.iknown'
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(':business')
}
